[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node --experimental-sqlite backend/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
